export interface userSettings{
      id: string;
      name : string;
      displayName : string;
      surname : string;
      firstName : string;
      email: string;
      settings : settings;
}
export interface settings{
      keyUiLocale : string;
     
}